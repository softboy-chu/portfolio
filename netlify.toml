[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  # Remove the --include=dev flag as it can cause conflicts in some environments
  NODE_VERSION = "22.22.2"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200